# Optional. Without a key the demo runs entirely on the scripted provider.
# Copy this file to .env.local and paste your key to switch the AI layer to a real model.
VITE_OPENROUTER_API_KEY=
VITE_OPENROUTER_MODEL=openai/gpt-4o-mini
